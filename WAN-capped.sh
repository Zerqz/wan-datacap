#!/bin/bash

#Running on a pfSense box with WAN as 4G, and opt1 as DSL fail-over after cap has been reached.
#Required pfSense packages: "sudo","bandwidthd","cron'


#Run as cronjob on the 1st of each month, or when cap has been reached (running every 5 minutes to check if close to cap)



#grep interface useage stats from bandwidthd.



#Shutdown interface if alloted amount of data is reached.



#Reset on the 1st of each month.


